package dev.noaa.backend;
import dev.noaa.backend.model.Station;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import org.locationtech.jts.geom.Point;
import org.locationtech.jts.geom.GeometryFactory;
import org.locationtech.jts.geom.Coordinate;
import org.locationtech.jts.geom.Point;
import java.util.List;
public class JsonSerializerTest {
    // @Test
    // void testConstructor() {
    //     GeometryFactory geometryFactory = new GeometryFactory();
    //     Point point = geometryFactory.createPoint(new Coordinate(0.5, 0.0));
    //     Station station = new Station("id", "name", point);
    //     assertEquals("id", station.getId());
    //     assertEquals("name", station.getName());
    //     assertEquals(point, station.getGeom());


    // }

}

